" turn on syntax highlighting
syntax on
" turn on hybrid line numbering on
set number relativenumber

" turn off vi compatibility
set nocompatible
" turn on being able to backspace over pre-existing characters
set backspace=2
